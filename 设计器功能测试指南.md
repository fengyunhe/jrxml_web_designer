# 设计器功能测试指南

## 📋 测试目标

从用户使用角度测试设计器功能，验证是否能通过可视化界面正确生成JRXML。

---

## 🚀 启动设计器

### 步骤1：启动开发服务器

```bash
cd /Users/yan.yang/open/jrxml_web_designer
npm run dev
```

### 步骤2：访问设计器

打开浏览器访问：`http://localhost:5173`

---

## 🧪 测试场景

### 场景1：创建Frame元素

**操作步骤**：
1. 从左侧元素库拖拽Frame到画布
2. 选中Frame元素
3. 在右侧属性面板中编辑Frame属性：
   - 设置布局模式（FreeLayout/HorizontalLayout/VerticalLayout）
   - 设置条件打印表达式（例如：`$V{PAGE_NUMBER} > 1`）
   - 启用忽略分页
   - 启用打印重复值
   - 设置背景颜色
4. 在Frame中添加子元素（例如：TextField、StaticText）

**验证点**：
- [ ] Frame属性面板正确显示
- [ ] 所有属性可以正常编辑
- [ ] 属性值正确保存
- [ ] 导出的JRXML包含所有属性

---

### 场景2：创建Table元素

**操作步骤**：
1. 从左侧元素库拖拽Table到画布
2. 选中Table元素
3. 在右侧属性面板中编辑Table属性：
   - 设置数据集名称
   - 设置查询语句
   - 设置表格样式（表头、列头、详情）
   - 启用打印表头
   - 启用忽略分页
4. 管理列：
   - 添加新列
   - 删除列
   - 编辑列名和宽度
   - 配置列头和详情单元格

**验证点**：
- [ ] Table属性面板正确显示
- [ ] 所有属性可以正常编辑
- [ ] 列管理功能正常
- [ ] 导出的JRXML包含所有属性

---

### 场景3：创建TextField元素

**操作步骤**：
1. 从左侧元素库拖拽TextField到画布
2. 选中TextField元素
3. 在右侧属性面板中编辑TextField属性：
   - 设置表达式（例如：`$F{fieldName}`）
   - 设置格式化模式（例如：`#,##0.00`）
   - 设置求值时间（Now/Report/Page/Column/Group/Band/Auto）
   - 设置超链接类型（None/Reference/Anchor）
   - 设置超链接URL表达式
   - 设置书签层级
   - 启用忽略分页
   - 启用空值时显示空白

**验证点**：
- [ ] TextField属性面板正确显示
- [ ] 所有属性可以正常编辑
- [ ] 属性值正确保存
- [ ] 导出的JRXML包含所有属性

---

### 场景4：创建Rectangle元素

**操作步骤**：
1. 从左侧元素库拖拽Rectangle到画布
2. 选中Rectangle元素
3. 在右侧属性面板中编辑Rectangle属性：
   - 启用打印重复值
   - 设置条件打印表达式

**验证点**：
- [ ] Rectangle属性面板正确显示
- [ ] 打印重复值开关正常
- [ ] 条件打印表达式编辑器正常
- [ ] 导出的JRXML包含所有属性

---

### 场景5：创建Ellipse元素

**操作步骤**：
1. 从左侧元素库拖拽Ellipse到画布
2. 选中Ellipse元素
3. 在右侧属性面板中编辑Ellipse属性：
   - 启用打印重复值
   - 设置条件打印表达式

**验证点**：
- [ ] Ellipse属性面板正确显示
- [ ] 打印重复值开关正常
- [ ] 条件打印表达式编辑器正常
- [ ] 导出的JRXML包含所有属性

---

### 场景6：创建Line元素

**操作步骤**：
1. 从左侧元素库拖拽Line到画布
2. 选中Line元素
3. 在右侧属性面板中编辑Line属性：
   - 启用打印重复值
   - 设置条件打印表达式

**验证点**：
- [ ] Line属性面板正确显示
- [ ] 打印重复值开关正常
- [ ] 条件打印表达式编辑器正常
- [ ] 导出的JRXML包含所有属性

---

### 场景7：创建Break元素

**操作步骤**：
1. 从左侧元素库拖拽Break到画布
2. 选中Break元素
3. 在右侧属性面板中编辑Break属性：
   - 启用重置页码

**验证点**：
- [ ] Break属性面板正确显示
- [ ] 重置页码开关正常
- [ ] 导出的JRXML包含所有属性

---

## 📤 导出JRXML

### 步骤1：完成设计

完成所有元素的添加和属性编辑。

### 步骤2：导出JRXML

1. 点击工具栏的"导出"按钮
2. 选择保存位置
3. 保存JRXML文件

### 步骤3：验证JRXML

打开导出的JRXML文件，检查：

- [ ] XML声明正确
- [ ] jasperReport元素存在
- [ ] UUID属性存在（标准格式）
- [ ] 所有必需属性完整
- [ ] 所有新属性正确生成
- [ ] 样式定义正确
- [ ] 字段定义正确
- [ ] Frame元素正确
- [ ] Table元素正确
- [ ] TextField元素正确
- [ ] Rectangle/Ellipse/Line/Break元素正确

---

## 📊 测试检查清单

### 1. 属性面板功能

- [ ] Frame属性面板正确显示
- [ ] Table属性面板正确显示
- [ ] TextField属性面板正确显示
- [ ] Rectangle属性面板正确显示
- [ ] Ellipse属性面板正确显示
- [ ] Line属性面板正确显示
- [ ] Break属性面板正确显示

### 2. 属性编辑功能

- [ ] 条件打印表达式编辑器正常
- [ ] 分页控制开关正常
- [ ] 打印控制开关正常
- [ ] 布局模式选择正常
- [ ] 超链接类型选择正常
- [ ] 书签层级输入正常
- [ ] 格式化模式输入正常
- [ ] 求值时间选择正常

### 3. 列管理功能

- [ ] 添加列功能正常
- [ ] 删除列功能正常
- [ ] 编辑列名功能正常
- [ ] 编辑列宽功能正常

### 4. JRXML生成

- [ ] 导出功能正常
- [ ] JRXML格式正确
- [ ] 所有属性正确生成
- [ ] UUID格式正确
- [ ] 与JasperStudio兼容

---

## 🎯 测试报告

完成测试后，请填写以下测试报告：

### 测试结果

| 测试项 | 状态 | 备注 |
|-------|------|------|
| Frame属性面板 | | |
| Table属性面板 | | |
| TextField属性面板 | | |
| Rectangle属性面板 | | |
| Ellipse属性面板 | | |
| Line属性面板 | | |
| Break属性面板 | | |
| 列管理功能 | | |
| JRXML导出 | | |
| JRXML格式 | | |

### 问题记录

| 问题 | 严重度 | 描述 | 状态 |
|-----|--------|------|------|
| | | | |
| | | | |

### 总体评价

- 功能完整性：⭐⭐⭐⭐⭐
- 用户体验：⭐⭐⭐⭐⭐
- 代码质量：⭐⭐⭐⭐⭐

---

## 📝 测试总结

**测试日期**：2026-05-28
**测试人员**：用户
**测试状态**：进行中

**核心结论**：
- [ ] 设计器功能完整
- [ ] 用户体验良好
- [ ] JRXML生成正确
- [ ] 代码风格统一

---

**测试指南版本**：v1.0
**创建日期**：2026-05-28
